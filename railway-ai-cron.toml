# RailwayでAI領収書確認専用のCronサービスを作る場合の設定例です。
# 同じGitHubリポジトリから追加サービスを作成し、この設定に相当する値を入れてください。
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python manage.py process_pending_receipts --limit 50"
cronSchedule = "*/10 * * * *"
restartPolicyType = "NEVER"
